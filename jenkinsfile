pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("nom-de-l-image:${env.BUILD_ID}")
                }
            }
        }
        stage('Run Docker Container') {
            steps {
                script {
                    docker.image("nom-de-l-image:${env.BUILD_ID}").run("-p 9000:9000 --name nom-du-conteneur")
                }
            }
        }
    }

    post {
        always {
            script {
                docker.image("nom-de-l-image:${env.BUILD_ID}").stop()
                docker.image("nom-de-l-image:${env.BUILD_ID}").remove()
            }
        }
    }
}