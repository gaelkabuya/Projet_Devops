pipeline {
    agent any
    stage('Build Docker Image') {
  steps {
    sh 'docker build -t monapp .'
  }
}

stage('Run Docker Container') {
  steps {
    sh 'docker run -p 9000:9000 monapp'
  }
}
}